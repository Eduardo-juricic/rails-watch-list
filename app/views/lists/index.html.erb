<h1>Minhas Listas</h1>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
    </li>
  <% end %>
</ul>

<%= link_to "Criar Nova Lista", new_list_path, class: "btn btn-primary" %>
